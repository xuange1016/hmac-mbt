# hmac-mbt 20 个有效 Commit 路线

1. 初始化项目元数据、仓库地址和忽略规则。
2. 增加竞赛材料和 20 步开发路线。
3. 建立 HMAC API 骨架和字节工具函数。
4. 增加基础测试和验证闭环。
5. 接入 SHA256 依赖并验证包配置。
6. 实现 key 规范化：短 key、等长 key、长 key。
7. 实现 inner pad 和 outer pad 生成。
8. 实现 HMAC-SHA256 主流程。
9. 增加 RFC 4231 基础测试向量。
10. 增加空 key 和空 message 边界测试。
11. 增加长 key 测试向量。
12. 实现恒定时间比较。
13. 增加 tag 长度不一致验证测试。
14. 增加十六进制辅助输出，便于 README 示例展示。
15. 完善 README API 文档和示例。
16. 增加 CI 配置，覆盖 `moon check` 和 `moon test`。
17. 更新验收说明和当前能力记录。
18. 整理许可证、参考标准和来源说明。
19. 增加更多互操作性说明和 Python 对照示例。
20. 申报前最终自查，确认仓库状态和材料一致。

每个 commit 必须对应真实开发动作，提交前优先执行 `moon check` 和 `moon test`。
